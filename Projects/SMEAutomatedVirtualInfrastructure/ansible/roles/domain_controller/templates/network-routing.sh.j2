#!/bin/bash
# SME Starter Infra: gateway routing helper
#
# In the Vagrant project environment this script is a lightweight placeholder.
# In a real SME deployment this is where you would add any
# custom routing checks or diagnostics for dc-1 acting as a
# gateway (iptables rules are already managed separately).

exit 0

