spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/networkops}
    username: ${DATABASE_USERNAME:networkops}
    password: ${DATABASE_PASSWORD:networkops}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: false

logging:
  level:
    com.networkops.dashboard: DEBUG
