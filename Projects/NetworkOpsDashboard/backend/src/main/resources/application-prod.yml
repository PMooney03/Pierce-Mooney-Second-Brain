# Production profile — values come from environment / secrets, never from Git.
spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
    driver-class-name: org.postgresql.Driver

logging:
  level:
    root: WARN
    com.networkops.dashboard: INFO
